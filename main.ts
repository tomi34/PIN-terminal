function omár2 () {
    if (734 < input.magneticForce(Dimension.Strength)) {
    	
    }
}
basic.forever(function () {
    if (pins.analogReadPin(AnalogPin.P0) < 341) {
    	
    } else if (pins.analogReadPin(AnalogPin.P0) > 682) {
    	
    } else {
    	
    }
})
